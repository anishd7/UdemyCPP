using namespace std;
#include <iostream>

int h2main() {
	int n, input, answer;
	cin >> n;
	if (n == 0)
		return 0;

	cin >> input;
	answer = input;
	n--;
	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}

	cin >> input;
	n--;
	if (input > answer)
		answer = input;

	if (n == 0) {
		cout << answer << endl;
		return 0;
	}
	

	return 0;
}