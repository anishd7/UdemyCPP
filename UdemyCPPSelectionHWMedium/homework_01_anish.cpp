using namespace std;
#include <iostream>

int h1main() {
	int input, answer;
	cin >> input;
	answer = input;

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cin >> input;
	if (input > answer) {
		answer = input;
	}

	cout << answer << endl;
	return 0;
}